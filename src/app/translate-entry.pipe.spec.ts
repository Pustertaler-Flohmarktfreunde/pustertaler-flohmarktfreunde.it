import { TranslateEntryPipe } from './translate-entry.pipe';

describe('TranslateEntryPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateEntryPipe();
    expect(pipe).toBeTruthy();
  });
});
